//Notacion 
//;---> delimita el termino de una linea, (No simpre se usa )
//Ejemplo
const a = 1;
a;
console.log(a);

//.  ---> Se utiliza para acceder a los atributos de un objeto
//Ejemplo
alumno.edad;

//[] ---> square brackets
// declarar listas, arrays o vector 
//Ejemplo de la declaracion 
const ar =[1, 2, 3, 4];
//acceder
ar[2];
console.log (ar[2]);// accediendo para imprimir 

//() ---> parenthesis function
function promedio(a, b, c, d){
    //toda la funcion 
}
for(var a = 4, a > if, i++){
    //Bucle 
}
if (elt a, b, c, d){
    //funtion 
}

//{} --> curly brackets
// me permiten  realizar objetos, funciones y estructuras  de control 
const alumno={
    adad:15,
    }

    for(var a = 4, a > if, i++){
        //Bucle 
    }
    
    if (elt a, b, c, d){
        // Estructur de control 
    }else{
        //Funcion
    }

